

```powershell
根据您的指导，我有如下思考，请您指导、评价、纠错：

一、启动 .\examples\gorogio\hello 后。

（1）打开了一个主窗口，

（2）主窗口内出现了一帧画面，

（3）画面组成：
3.1 component # 提供：有交互和复合能力的 UI 单元（有行为、有状态、组合出来）
3.2 primitive # 提供：最基础的、无状态的视觉元素。
3.3 container # 负责空间关系：东西怎么摆？
3.4 modifier  # 负责外观修饰：东西怎么装饰？

（4）事件触发：
4.1 鼠标点击画面中的某个元素，触发程序按照代码逻辑，绘制下一帧。
4.2 鼠标点击画面中的某个元素，触发程序按照代码逻辑，新开一个窗口，绘制新窗口的画面。

（5）关闭所有窗口，退出程序。

二、您说我下一步应该做：v0.3.1 Component Tree，您的意思是不是
（1）长期积累 目录 component 内的资产:UI 单元。
（2）长期积累 目录 primitive 内的资产：视觉元素。
（3）补全和完善 container 和 modifier 内对 gio 底层api 的简化使用。
（4）请问 component/counter.go 是否有资格成为 component 内的资产，和 table 之类同级别呢？
type Counter struct {
    state *CounterState
    increment *Button
    reset *Button
}
（5）以后，.\examples\gorogio\ 下的所有案例和实操，是不是只要三个层级：main.go，runtime.go，xxx_page.go，yyy_page.go 就够了。
（6）第（5）条，是不是我们的目标，通过学习和实操 gio，得到 go 的不断深入、gio的资产长期积累。
（7）另外，文件和目录：.\state\state.go  是不是要删除。
（8）另外，文件和目录：.\style\theme.go  是需要的，但是当下先不深入。
```

