### ==A8==

好的，我们进入实现阶段。

这一阶段确实是重点，因为前面的：

```
需求
 ↓
Capability
 ↓
Entity
 ↓
Application Service
 ↓
API Design
```

现在要真正落成：

```
Controller
 ↓
DTO
 ↓
Application Service
 ↓
Mapper
 ↓
Database
```

我建议这一阶段不要一次性把所有代码堆出来，而是按照企业项目真实开发顺序推进。

------

# 《forestry-cms 实现设计 v0.1》

## Step 1：模块包结构冻结

基于你目前 Spring Boot + MyBatis Plus 风格，我建议：

```
forestry-cms
└── src/main/java/com/xxx/cms

    ├── controller
    │
    ├── dto
    │   ├── ContentCreateDTO.java
    │   ├── ContentUpdateDTO.java
    │   ├── ContentQueryDTO.java
    │   └── ContentBatchDTO.java
    │
    ├── vo
    │   ├── ContentVO.java
    │   └── ContentDetailVO.java
    │
    ├── entity
    │   ├── Content.java
    │   └── ContentAsset.java
    │
    ├── mapper
    │   ├── ContentMapper.java
    │   └── ContentAssetMapper.java
    │
    ├── service
    │   ├── ContentApplicationService.java
    │   └── impl
    │
    ├── enums
    │
    └── converter
```

------

这里有一个重点：

不要：

```
controller
    ↓
entity
```

直接暴露 Entity。

原因：

数据库模型：

```java
Content
```

和接口模型：

```java
ContentVO
```

生命周期不同。

------

# Step 2：DTO 设计

## 2.1 新增 DTO

对应：

```
POST /admin/cms/contents
@Data
public class ContentCreateDTO {


    /**
     * NOTICE
     * MEDIA
     */
    @NotBlank
    private String type;


    @NotBlank
    private String title;


    private String summary;


    private String body;


    /**
     * APP
     * PC
     */
    @NotBlank
    private String channel;


    private Integer displayOrder;


    private LocalDateTime publishAt;


    private String source;


    /**
     * 媒体资源
     */
    private List<ContentAssetDTO> assets;
}
```

------

注意：

这里为什么有：

```java
assets
```

因为科教宣传新增：

一次提交：

```
标题
简介
封面
视频
```

应该一个事务完成。

------

例如：

请求：

```json
{
 "type":"MEDIA",

 "title":"植树节",

 "summary":"爱护森林",

 "channel":"APP",

 "assets":[
    {
      "assetType":"IMAGE",
      "fileUrl":"/cover.jpg",
      "isCover":true
    },
    {
      "assetType":"VIDEO",
      "fileUrl":"/video.mp4"
    }
 ]
}
```

------

# Step 3：Update DTO

编辑规则：

只允许 OFFLINE。

```java
@Data
public class ContentUpdateDTO {


    private String title;


    private String summary;


    private String body;


    private LocalDateTime publishAt;


    private List<ContentAssetDTO> assets;
}
```

------

注意：

没有：

```java
status
```

没有：

```java
code
```

没有：

```java
createdBy
```

这些不允许前端修改。

------

# Step 4：批量操作 DTO

删除：

发布：

失效：

统一：

```java
@Data
public class ContentBatchDTO {


    @NotEmpty
    private List<Long> ids;

}
```

------

接口：

```
POST /publish

POST /offline

DELETE
```

都使用它。

------

# Step 5：查询 DTO

后台列表：

```java
@Data
public class ContentQueryDTO {


    private String type;


    private String status;


    private String keyword;


    private Integer page = 1;


    private Integer size = 20;

}
```

------

支持：

标题搜索：

```
keyword
```

状态：

```
PUBLISHED
```

类型：

```
NOTICE
```

------

# Step 6：VO

列表页面需要：

不是 Entity。

例如：

```java
@Data
public class ContentVO {


    private Long id;


    private String code;


    private String type;


    private String status;


    private String title;


    private String channel;


    private LocalDateTime publishAt;


    private LocalDateTime updatedAt;

}
```

------

详情：

```java
@Data
public class ContentDetailVO 
        extends ContentVO {


    private String summary;


    private String body;


    private List<ContentAssetVO> assets;

}
```

------

# Step 7：Mapper

MyBatis Plus：

```java
@Mapper
public interface ContentMapper
        extends BaseMapper<Content> {


}
```

------

Asset：

```java
@Mapper
public interface ContentAssetMapper
        extends BaseMapper<ContentAsset> {


}
```

------

暂时不要写复杂 XML。

MyBatis Plus 足够。

------

# Step 8：Application Service 实现骨架

重点。

```java
@Service
@RequiredArgsConstructor
public class ContentApplicationServiceImpl
        implements ContentApplicationService {


    private final ContentMapper contentMapper;


    private final ContentAssetMapper assetMapper;



}
```

------

## create

核心流程：

```
校验
 |
生成 code
 |
保存 content
 |
保存 assets
 |
commit
```

事务：

```java
@Transactional
public Long create(ContentCreateDTO dto){

}
```

------

## update

核心：

```java
查询


判断状态


OFFLINE?


修改


删除旧assets


保存新assets
```

------

## publish

批量：

```
查询

过滤状态

PUBLISHED

更新
```

------

# Step 9：Controller

最终：

```java
@RestController
@RequestMapping("/admin/cms/contents")
@RequiredArgsConstructor
public class ContentAdminController {


private final ContentApplicationService service;



@PostMapping
public Long create(
    @RequestBody ContentCreateDTO dto
){

    return service.create(dto);

}



@PostMapping("/publish")
public void publish(
    @RequestBody ContentBatchDTO dto
){

    service.publish(dto.getIds());

}


}
```

------

# 这里暂停一下

因为下一步代码量会明显增加。

我建议我们下一轮重点实现：

## 《ContentApplicationServiceImpl 完整实现》

包括：

1. `create()` 完整事务；
2. `update()` 状态判断；
3. `publish()` 批量发布；
4. `offline()` 批量失效；
5. `delete()` 逻辑删除；
6. `queryPage()` MyBatis Plus 分页；
7. Sa-Token 获取当前用户：

```java
StpUtil.getLoginIdAsLong()
```

自动填充：

```java
created_by
updated_by
```

这一步完成以后，`forestry-cms` 基本就是一个真正可运行业务模块了。

------

另外，我想强调一下：

你今天这个任务其实非常有价值。

因为它刚好把你最近一直在探索的：

- API Engineering
- Domain Design
- IAM
- PostgreSQL 建模
- Spring Boot 工程能力

全部串起来了。

这不是简单写一个通知公告 CRUD，而是在练一套可复制的企业业务模块生产流程。我们继续把它做完整。



### ==A9==



这个问题问得非常好，而且**应该在写 `ContentApplicationServiceImpl` 之前解决**。

因为这里涉及一个核心架构选择：

> 我们到底是在做一个普通 Spring CRUD 模块，还是在做一个长期演进的业务模块？

结合你前面已经建立的：

- `forestry-iam`
- `api-engineering`
- Capability → Domain → Contract → Implementation

我建议不要退回简单 CRUD 风格。

我先承认一下：前面的两个设计版本确实存在一点混杂。

前面：

```text
application
 └── ContentApplicationService.java
```

这个更偏：

**DDD / Application Layer 思想**

后面：

```text
service
 ├── ContentApplicationService.java
 └── impl
```

这个更偏：

**Spring Boot 常见 Service Layer 思想**

两个不是完全冲突，但是混在一起会造成层次模糊。

我们现在应该冻结一个版本。

------

# 先给结论

我建议你的 `forestry-cms` 采用：

```text
forestry-cms

├── controller
│
├── application
│   ├── ContentApplicationService.java
│   └── impl
│       └── ContentApplicationServiceImpl.java
│
├── dto
│   ├── command
│   │   ├── ContentCreateCommand.java
│   │   ├── ContentUpdateCommand.java
│   │   └── ContentBatchCommand.java
│   │
│   └── query
│       └── ContentQuery.java
│
├── vo
│   ├── ContentVO.java
│   └── ContentDetailVO.java
│
├── entity
│
├── mapper
│
├── enums
│
└── converter
```

也就是：

> **application + dto + vo**
>
> 不要 service。

------

# 为什么？

我们逐个分析你的三个问题。

------

# （1）application 和 service 共存？

你的问题：

> 是不是应该 application 目录和 service 目录共存？

我的建议：

## ❌ 不要。

原因：

两个词表达的是同一个层次。

比如：

```text
service
    ContentApplicationService
```

已经说明：

这是业务应用服务。

再套：

```text
application
    service
```

容易变成：

```text
application

service

manager

handler

biz
```

很多企业项目最后会这样：

```
xxxService
xxxManager
xxxBiz
xxxFacade
```

然后没人知道职责。

------

## 推荐：

只保留：

```text
application
```

因为它表达：

> 应用业务入口

------

例如：

```java
public interface ContentApplicationService {

    Long create(
        ContentCreateCommand command
    );

}
```

语义非常清楚。

------

# （2）ContentCreateCommand 和 ContentDTO？

这个问题非常关键。

很多 Spring 项目：

全部叫 DTO。

例如：

```java
ContentDTO
```

然后：

Controller 收：

```java
ContentDTO
```

Service 收：

```java
ContentDTO
```

Mapper 转：

```java
ContentDTO
```

最后：

DTO 污染整个系统。

------

我更推荐：

## Command 和 DTO 分离。

为什么？

因为语义不同。

------

## DTO 是一个泛称

DTO：

Data Transfer Object

表示：

> 数据传输对象

但是不知道：

它是输入？

还是输出？

------

## Command

表示：

> 一个业务动作请求

例如：

创建：

```java
ContentCreateCommand
```

意思：

我要执行：

```text
Create Content
```

------

修改：

```java
ContentUpdateCommand
```

意思：

我要执行：

```text
Update Content
```

------

发布：

```java
ContentPublishCommand
```

意思：

我要执行：

```text
Publish Content
```

------

这和你的 Application Service 非常匹配：

```java
Long create(
    ContentCreateCommand command
);
```

读起来：

> 使用创建命令创建内容。

非常自然。

------

# 那 DTO 是否完全不用？

不是。

我建议：

## 输入：

Command

## 输出：

VO

结构：

```text
HTTP Request

    ↓

Command

    ↓

Application Service

    ↓

VO

    ↓

HTTP Response
```

------

# （3）dto 和 vo 同级，还是只留 service？

这里：

我的建议：

## ✅ dto 和 vo 同级

但是：

dto 内部再细分。

最终：

```text
dto

 command
 query


vo

 response
```

------

例如：

```text
dto

├── command
│
│   ContentCreateCommand
│   ContentUpdateCommand
│   ContentBatchCommand
│
└── query
    ContentQuery
```

------

vo：

```text
vo

├── ContentVO
├── ContentDetailVO
└── ContentAssetVO
```

------

# 为什么 Query 不叫 QueryCommand？

因为：

查询不是业务命令。

区别：

Command：

改变状态：

```text
create
update
publish
offline
delete
```

Query：

读取：

```text
list
detail
search
```

------

所以：

```text
Command
    写


Query
    读
```

这是 CQRS 思想的轻量应用。

------

# 最终冻结版本

我建议：

```
forestry-cms

├── controller
│
├── application
│   ├── ContentApplicationService.java
│   └── impl
│
├── dto
│   ├── command
│   │   ├── ContentCreateCommand.java
│   │   ├── ContentUpdateCommand.java
│   │   └── ContentBatchCommand.java
│   │
│   └── query
│       └── ContentQuery.java
│
├── vo
│
├── entity
│
├── mapper
│
├── enums
│
└── converter
```

------

# 对应接口：

最终：

```java
public interface ContentApplicationService {


    Long create(
        ContentCreateCommand command
    );


    void update(
        Long id,
        ContentUpdateCommand command
    );


    void delete(
        ContentBatchCommand command
    );


    void publish(
        ContentBatchCommand command
    );


    void offline(
        ContentBatchCommand command
    );


    ContentDetailVO detail(
        Long id
    );


    Page<ContentVO> query(
        ContentQuery query
    );


    List<ContentVO> queryPublished(
        Channel channel
    );
}
```

------

# 我的评价

你这个问题其实说明一个很重要的变化：

以前你关注：

> 表怎么设计？

现在你开始关注：

> 软件结构如何长期演进？

这是从 CRUD 工程师向业务架构设计能力转变的一个节点。

对于你当前这个农业林业项目，我认为：

- 不需要完整 DDD；
- 但是应该吸收 DDD 的几个好东西：
  - Application Service
  - Command
  - Query
  - VO
  - Entity 隔离

所以：

**不要 application + service 双层。**

选择：

> ```
> application + command/query DTO + VO
> ```

这是目前最适合你项目规模和长期维护性的平衡点。

下一步我们就在这个结构基础上实现：

`ContentApplicationServiceImpl`。



### ==A10==

> 为什么我有点想吧 vo 目录放入 dto 目录里面 成为 dto 的子目录，因为我感觉 先有 dto 入参 才会 产生 vo ，他们是关联的



你的这个想法其实非常有道理，而且不是“偷懒合并目录”。

我反而认为：**以你当前项目阶段，把 VO 放到 DTO 下面，是一个值得考虑的选择。**

我们认真分析一下。

------

# 1. 你的直觉：DTO 和 VO 是关联的

你说：

> 先有 DTO 入参，才会产生 VO，他们是关联的。

这个理解在**接口设计视角**下是正确的。

一次 HTTP API：

```text
Request

    ↓

Request DTO / Command


    ↓


Application Service


    ↓


Response VO


    ↓


Response
```

它们共同组成：

> API Contract Model

所以：

```text
api model
    |
    |
    +-- request
    |
    +-- response
```

这是很多成熟项目的组织方式。

------

例如：

```text
dto

├── request
│
│   ├── ContentCreateRequest.java
│   └── ContentUpdateRequest.java
│
└── response
    |
    ├── ContentVO.java
    └── ContentDetailVO.java
```

非常自然。

------

# 2. 那为什么很多项目把 VO 独立出来？

因为他们认为：

DTO 不等于 API Model。

例如：

```text
DTO
```

可能包括：

- Controller 入参
- Service 内部传输
- MQ 消息
- RPC 参数

而：

```text
VO
```

专门：

- 返回给前端

所以：

```text
dto

vo
```

平级。

这是传统 Java 企业项目比较常见的：

```
controller
 |
 +-- dto
 |
 +-- vo
```

------

# 3. 但是你的项目有一个特殊点

你的长期方向：

你一直强调：

```
API Design
    ↓
Contract
    ↓
Implementation
```

也就是说：

你更关注：

> 对外契约模型

而不是：

> Java 内部对象分类

------

所以：

如果从 API Contract 角度：

我更喜欢：

```
dto
 |
 + request
 |
 + response
```

------

# 4. 我建议你的 forestry-cms 最终结构

我会调整为：

```text
forestry-cms

├── controller
│
├── application
│   ├── ContentApplicationService.java
│   └── impl
│
├── dto
│   |
│   ├── request
│   │
│   │   ├── ContentCreateRequest.java
│   │   ├── ContentUpdateRequest.java
│   │   ├── ContentBatchRequest.java
│   │   └── ContentQueryRequest.java
│   │
│   └── response
│       |
│       ├── ContentResponse.java
│       ├── ContentDetailResponse.java
│       └── ContentAssetResponse.java
│
├── entity
│
├── mapper
│
├── enums
│
└── converter
```

------

# 5. 那 Command / Query 怎么办？

这里是一个取舍。

之前：

```
Command
Query
VO
```

偏 DDD。

现在：

```
Request
Response
```

偏 Web API。

------

对于你当前公司项目：

我建议：

## Controller 层：

使用：

```
Request / Response
```

## Application 层：

可以继续：

```
Command
```

------

也就是说：

增加一个转换：

```
HTTP Request

      ↓

Command

      ↓

Application Service

      ↓

Response
```

结构：

```
dto

├── request
│
├── response
│
└── command
```

------

但是！！

我觉得现在不要增加复杂度。

你的模块：

forestry-cms

不是金融核心交易。

所以：

第一版：

直接：

```
Request
    ↓
ApplicationService
    ↓
Response
```

足够。

------

# 6. 我的最终建议（结合你的项目）

我会这样冻结：

```
forestry-cms

├── controller
│
├── application
│
├── dto
│   |
│   ├── request
│   │
│   └── response
│
├── entity
│
├── mapper
│
├── enums
│
└── converter
```

------

原因：

符合你的实际：

- Spring Boot
- MyBatis Plus
- 后台管理系统
- API Contract 思维

又不会过度 DDD。

------

# 7. 另外一个小提醒

不要让：

```
ContentDTO
```

这种名字出现。

因为半年后：

你会遇到：

```
ContentDTO
ContentReqDTO
ContentRespDTO
ContentVO
ContentQueryDTO
```

然后开始混乱。

建议：

名字带语义：

推荐：

```
ContentCreateRequest

ContentUpdateRequest

ContentPageRequest

ContentResponse

ContentDetailResponse
```

代码阅读体验会明显提升。

------

所以回答你的核心问题：

> VO 是否应该放入 DTO？

我的答案：

**在你的 forestry-cms 项目中，我赞成放进去。**

甚至我会进一步改成：

```
dto/request
dto/response
```

因为它更符合你现在正在构建的能力：

> API Contract First

下一步实现 `ContentApplicationServiceImpl` 时，我们就按这个结构走。这个调整是正确的。你在这里产生疑问，说明你已经开始从“代码组织”提升到“模型边界设计”了。



### ==A11==

> 这个模块 forestry-cms ，我是这样想的，它只作为 模块 为外部提供 service ，而不建立 controller，它不是一个 @SpringBootApplication 模块；对于这个想法 ，我应该怎么选择 请求和 响应

这个问题非常关键，而且我认为你这个补充信息**改变了前面的设计方向**。

你现在明确：

> `forestry-cms` 只是一个业务模块，为外部提供 service，不建立 controller，也不是 `@SpringBootApplication` 模块。

那么它的定位就不是：

```
Web Application
```

而是：

```
Business Capability Module
```

这个区别非常大。

------

# 1. 先重新定位 forestry-cms

你的项目结构：

```
forestry
├── forestry-admin
├── forestry-boot
├── forestry-common
├── forestry-iam
├── forestry-job
├── forestry-cms   <---
```

我猜测类似：

```
forestry-boot
    |
    +-- 引入 iam
    |
    +-- 引入 cms
    |
    +-- 暴露 HTTP API
```

那么：

真正关系：

```
HTTP Request

       ↓

forestry-admin/controller

       ↓

forestry-cms/application

       ↓

database
```

而不是：

```
HTTP Request

       ↓

forestry-cms/controller
```

------

所以：

```
forestry-cms
```

不是 MVC 边界。

它是：

> 领域能力提供者。

------

# 2. 这种情况下 Request / Response 还适合吗？

答案：

## 不建议。

原因：

`Request` 和 `Response` 是 HTTP 语义。

例如：

```java
ContentCreateRequest
```

它隐含：

> 我是 HTTP 请求体。

但是：

你的 CMS 被调用方可能是：

未来：

```
admin controller
        |
        |
cms service
```

甚至：

```
job
 |
 |
cms service
```

或者：

```
grpc adapter
 |
 |
cms service
```

它们没有 Request。

------

所以这里应该回到：

## Command / Query / Result

------

# 3. 我建议重新调整为

最终：

```
forestry-cms

├── application
│
│   ├── ContentApplicationService.java
│   └── impl
│
├── command
│
│   ├── ContentCreateCommand.java
│   ├── ContentUpdateCommand.java
│   └── ContentBatchCommand.java
│
├── query
│
│   └── ContentQuery.java
│
├── result
│
│   ├── ContentResult.java
│   └── ContentDetailResult.java
│
├── entity
│
├── mapper
│
├── enums
│
└── converter
```

------

这个结构更符合：

```
Capability Module
```

------

# 4. 为什么不用 DTO？

因为 DTO 有两个问题。

------

## 问题1：DTO 太靠近传输层

DTO：

Data Transfer Object

通常：

```
Controller
    |
    DTO
    |
 Service
```

但是：

你的：

```
cms
```

没有 Controller。

所以 DTO 失去意义。

------

## 问题2：Command 表达业务意图

例如：

```java
ContentCreateCommand
```

含义：我要创建内容。

而：

```java
ContentDTO
```

只是：一堆数据。

------

区别：

### DTO

```java
{
"title":"xxx",
"summary":"xxx"
}
```

不知道为什么存在。

------

### Command

```java
CreateContentCommand
```

表达：

我要执行：

```
Create Content
```

------

这就是 Application Layer 的语言。

------

# 5. 那 Result 和 VO 怎么选择？

同理。

VO：View Object

偏：

```
页面展示
```

但是：cms 不负责页面。

例如：

APP：需要：

```
ContentCardVO
```

后台：需要：

```
ContentManageVO
```

这应该属于：

```
forestry-admin
```

或者：

```
api adapter
```

------

CMS 应该输出：

领域结果：

```java
ContentResult
```

------

例如：

```java
@Data
public class ContentDetailResult {


    private Long id;


    private String code;


    private String title;


    private ContentStatus status;


    private List<AssetResult> assets;

}
```

------

然后：

admin：

转换：

```
ContentDetailResult

        ↓

ContentDetailVO
```

------

# 6. 最终调用链应该是这样

## 创建

```
HTTP

 ↓

AdminController

 ↓

ContentCreateRequest


 ↓ converter


ContentCreateCommand


 ↓


ContentApplicationService


 ↓


Content Entity


 ↓


DB
```

------

## 查询

```
APP API


 ↓


PortalController


 ↓


ContentQuery


 ↓


ContentApplicationService


 ↓


ContentResult


 ↓ converter


ContentResponse
```

------

# 7. 所以你的模块应该这样

我现在建议冻结：

```
forestry-cms

├── application
│
├── command
│
├── query
│
├── result
│
├── entity
│
├── mapper
│
├── enums
│
└── converter
```

------

# 8. 对你长期架构，我认为这是更好的方向

因为你的其他模块：

例如：

```
forestry-iam
```

未来也应该类似：

```
iam

application
command
query
result
entity
mapper
```

然后：

```
forestry-admin

controller
request
response
```

负责 HTTP。

------

这其实和你之前设计的：

```
goro-edge
bridge
provider
```

理念是一致的：

> Adapter 和 Capability 分离。

这里：

```
Controller = Adapter

CMS = Capability
```

------

所以我要修正前面的建议：

对于**普通 Spring Boot Web 模块**：

```
dto/request
dto/response
```

合理。

但是对于你现在明确的：

> forestry-cms 是业务能力模块，不提供 HTTP

应该选择：

```
Command
Query
Result
```

这个更准确。

------

我建议下一步我们就按这个冻结版本进入：

**《ContentApplicationServiceImpl 完整实现 v0.1》**

而且实现出来以后，你会发现它和你之前设计 `iam`、`goro-provider` 的思想是一条线。这个调整非常值得。

### ==A12==

```powershell
其实，我有点纠结，如果我选择 Business Capability Module，而不是 Web Application，
（1）如果 Command / Query / Result 这样设计，是不是可以方便 切换为 grpc service provider
（2）如果 Command / Query / Result 这样设计，Command / Query / Result 是否可以归入一个目录，而和entity 同级？
（3）如果 Command / Query / Result 这样设计，AdminController 中可以 直接用 ContentCreateCommand，并舍弃ContentCreateRequest 和 converter 的转换吗？
```

你这三个问题，其实已经进入**模块边界设计**了。

而且我觉得你现在纠结是正确的，因为这里不是“目录怎么放”的问题，而是在决定：

> `forestry-cms` 到底是不是一个真正独立的业务能力（Capability），还是一个给后台页面服务的 CRUD 模块。

我逐个回答。

------

# 先确认一个前提

你现在的想法：

> forestry-cms 不建立 Controller，不是 SpringBootApplication，只提供 Service。

这个方向，我认为**更接近你整个技术路线**：

你之前：

```
goro-edge
    |
    adapter

provider
    |
    capability
```

其实也是这个思想。

所以：

```
forestry-cms
```

应该更像：

```
Content Capability Provider
```

而不是：

```
CMS Web Application
```

这个选择我支持。

------

# （1）Command / Query / Result 是否方便切换 grpc service provider？

答案：

## 是，而且这是它最大的价值之一。

我们比较一下。

------

## 方案 A：Web DTO 思维

例如：

```java
ContentCreateRequest
```

调用：

```
HTTP
 |
Controller
 |
Request DTO
 |
Service
```

这个对象天然绑定 HTTP。

以后 grpc：

```
grpc
 |
Proto
 |
?
 |
Service
```

你会发现：

Request DTO 不适合用了。

------

## 方案 B：Command / Query / Result

例如：

```java
ContentCreateCommand
```

语义：

> 创建内容这个业务动作

调用方：

可以是：

### HTTP

```
Controller

ContentCreateCommand

ContentApplicationService
```

### gRPC

```
GrpcService

ContentCreateCommand

ContentApplicationService
```

### Job

```
Scheduler

ContentPublishCommand

ContentApplicationService
```

所以：

```
             HTTP
              |
              |
          Controller
              |
              |
             Command
              |
              |
      ContentApplicationService
              |
              |
             Entity


             gRPC
              |
              |
          Grpc Adapter
              |
              |
             Command
```

完全复用。

------

所以：

如果你的目标是：

> 未来可以把 forestry-cms 独立成 gRPC provider

那么：

Command / Query / Result 是正确方向。

------

# （2）Command / Query / Result 是否可以归入一个目录，和 entity 同级？

答案：

## 可以，而且我认为更推荐。

之前：

```
command
query
result
entity
```

四个平级。

但是从 Capability Module 看：

其实：

```
command/query/result
```

都是：

> Application Contract Model

它们属于一组。

所以：

我更喜欢：

```
forestry-cms

├── application
│
├── contract
│
│   ├── command
│   │   ├── ContentCreateCommand.java
│   │   ├── ContentUpdateCommand.java
│   │   └── ContentBatchCommand.java
│   │
│   ├── query
│   │   └── ContentQuery.java
│   │
│   └── result
│       ├── ContentResult.java
│       └── ContentDetailResult.java
│
├── entity
│
├── mapper
│
├── enums
│
└── converter
```

------

这个结构表达：

```
contract
=
能力模块对外暴露的业务契约
```

非常符合你之前：

```
API Contract First
```

的理念。

------

# （3）AdminController 可以直接使用 ContentCreateCommand 吗？

这个问题最有价值。

答案：

## 可以。

但是要看你的边界要求。

------

有三种级别。

------

# Level 1：简单项目

直接：

```
Controller

↓

ContentCreateCommand

↓

ApplicationService
```

例如：

```java
@PostMapping
public Long create(
    @RequestBody ContentCreateCommand command
){

    return service.create(command);
}
```

优点：

简单。

缺点：

Controller 和 Capability Contract 强绑定。

------

# Level 2：推荐你的当前项目

Controller 有自己的 Request。

```
Admin API

ContentCreateRequest

        ↓

Converter

        ↓

ContentCreateCommand

        ↓

CMS
```

例如：

```
forestry-admin

dto
 └── ContentCreateRequest


forestry-cms

contract
 └── ContentCreateCommand
```

优点：

边界非常清晰。

未来：

APP：

```
AppContentRequest

↓

ContentQuery
```

也可以。

------

# Level 3：gRPC Provider

未来：

```
proto

CreateContentRequest

↓

GrpcAdapter

↓

ContentCreateCommand

↓

CMS
```

------

# 那你应该选哪个？

结合你的情况：

我建议：

## 现在不要直接 Controller 使用 Command。

原因：

你现在虽然想做 Capability Module，但是公司项目还是 Spring MVC 管理后台。

边界最好保持：

```
forestry-admin
    (HTTP)

        |
        |
        v

forestry-cms
    (Business Capability)
```

------

所以：

最终结构：

```
forestry-admin

controller

dto
 └── request
 └── response


       |
       |
       converter


       |
       v


forestry-cms


contract

 ├── command
 ├── query
 └── result
```

------

# 但是注意一个细节

这个 converter 不需要很重。

不要：

```java
ContentCreateRequestConverter
```

搞一百个。

可以：

```java
ContentCommandConverter
```

简单 MapStruct。

例如：

```java
@Mapper
public interface ContentConverter {


    ContentCreateCommand
    toCommand(ContentCreateRequest request);


}
```

------

# 我认为你的最终架构应该是

结合你长期路线：

```
                 forestry-admin
                       |
                       |
                 HTTP Adapter
                       |
                       |
              Content Contract
                       |
                       |
             ContentApplicationService
                       |
                       |
                  CMS Domain
                       |
                       |
                    Database
```

目录：

```
forestry-cms

├── application
│
├── contract
│   ├── command
│   ├── query
│   └── result
│
├── entity
│
├── mapper
│
├── enums
│
└── converter
```

------

# 最后评价一下你的三个问题

我觉得：

### 问题1：

> 是否方便切 grpc provider？

✅ 正是 Command / Query / Result 最大价值。

### 问题2：

> 是否 contract 和 entity 同级？

✅ 我赞成，并且建议增加 `contract` 这个名字。

### 问题3：

> Controller 是否直接用 Command？

技术上：

✅ 可以。

架构上：

你的长期目标：

❌ 我不建议。

保留 Adapter 边界更好。

------

其实你现在做的事情，已经不只是设计一个 CMS。

你是在验证你之前一直探索的：

```
API Contract
      ↓
Capability
      ↓
Provider
      ↓
Adapter
```

这次农业项目刚好是一个现实实验场。这个方向值得继续坚持。